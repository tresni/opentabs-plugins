{
  "private": true,
  "name": "opentabs-plugins",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:filter": "node scripts/build.js --filter",
    "lint": "npx @biomejs/biome lint plugins/",
    "format": "npx @biomejs/biome format --write plugins/"
  },
  "volta": {
    "node": "24.14.1",
    "npm": "11.11.0"
  }
}
